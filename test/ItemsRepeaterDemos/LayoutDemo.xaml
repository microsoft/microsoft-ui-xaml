<local:NavPage
    x:Class="ItemsRepeaterDemos.LayoutDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ItemsRepeaterDemos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mux="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <mux:ItemsRepeater x:Name="repeater">
                <DataTemplate>
                    <Button Content="{Binding}" />
                </DataTemplate>
                <mux:ItemsRepeater.Layout>
                    <mux:UniformGridLayout MinColumnSpacing="10" MinRowSpacing="10" MinItemWidth="100" MinItemHeight="100"/>
                </mux:ItemsRepeater.Layout>
            </mux:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</local:NavPage>
















<!-- mux:StackLayout Spacing="10" /-->
<!--<mux:UniformGridLayout MinColumnSpacing="10" MinRowSpacing="10" MinItemWidth="100" MinItemHeight="100"/>-->
<!--<mux:FlowLayout MinColumnSpacing="10" MinRowSpacing="10" LineAlignment="SpaceEvenly" />-->

<!--<local:NonVirtualStackLayout />-->
<!--<local:VirtualizingUniformStackLayout ItemWidth="100" ItemHeight="100"/>-->
