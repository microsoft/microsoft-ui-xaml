<Page
    x:Class="ItemsRepeaterDemos.ItemTemplateDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ItemsRepeaterDemos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mux="using:Microsoft.UI.Xaml.Controls"    
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <mux:ItemsRepeater x:Name="repeater">
                <DataTemplate>
                    <Button Content="{Binding}" />
                </DataTemplate>
            </mux:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
























<!--<DataTemplate>
                    <Button Content="{Binding}" />
                </DataTemplate>-->
<!--<local:MySelector>
                    <local:MySelector.Template1>
                        <DataTemplate>
                            <Button Content="{Binding}" Width="100" />
                        </DataTemplate>
                    </local:MySelector.Template1>
                    <local:MySelector.Template2>
                        <DataTemplate>
                            <Button Content="{Binding}" Width="150" />
                        </DataTemplate>
                    </local:MySelector.Template2>
                </local:MySelector>-->
