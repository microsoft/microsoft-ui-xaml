<Page
    x:Class="Flick.StoreScenario"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flick"
    xmlns:mux="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <mux:StackLayout Spacing="10"  x:Name="verticalStack"/>
        <mux:StackLayout Spacing="20" Orientation="Horizontal" x:Name="horizontalStack"/>
        <DataTemplate x:Name="itemTemplate">
            <UserControl IsTabStop="True" UseSystemFocusVisuals="True" GotFocus="OnItemGotFocus" LostFocus="OnItemLostFocus" Width="300" Height="200" >
                <Image Source="{Binding UrlMedium}" Stretch="UniformToFill" Tapped="OnItemClicked"  />
            </UserControl>
        </DataTemplate>
    </Page.Resources>
    <Grid Margin="10" KeyDown="Grid_KeyDown">
        <ScrollViewer x:Name="svOuter">
            <mux:ItemsRepeater x:Name="rootRepeater" Layout="{StaticResource verticalStack}">
                <DataTemplate>
                    <ScrollViewer
                        x:Name="svInner"
                        HorizontalScrollBarVisibility="Hidden"
                        VerticalScrollBarVisibility="Hidden"
                        HorizontalScrollMode="Enabled"
                        VerticalScrollMode="Disabled">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="28"/>
                            <mux:ItemsRepeater x:Name="repeater"
                                               ItemsSource="{Binding}"
                                               Layout="{StaticResource horizontalStack}"
                                               ItemTemplate="{StaticResource itemTemplate}"
                                               ElementPrepared="Repeater_ElementPrepared"/>
                        </StackPanel>
                    </ScrollViewer>
                </DataTemplate>
            </mux:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
