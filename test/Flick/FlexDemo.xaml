<Page
    x:Class="Flick.FlexDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flick"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:utilities="using:MUXControlsTestApp.Utilities"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Resources>
            <local:Flex x:Name="layout"/>
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <utilities:EntityPropertiesControl Entity="{StaticResource layout}" />
        <controls:LayoutPanel Grid.Column="1" Layout="{StaticResource layout}">
            <Button Width="200" Height="100">1</Button>
            <Button Width="200" Height="100">2</Button>
            <Button Width="200" Height="100">3</Button>
            <Button Width="200" Height="100">4</Button>
            <Button Width="200" Height="100">5</Button>
            <Button Width="200" Height="100">6</Button>
            <Button Width="200" Height="100">7</Button>
            <Button Width="200" Height="100">8</Button>
            <Button Width="200" Height="100">9</Button>
            <Button Width="200" Height="100">10</Button>
        </controls:LayoutPanel>
        
    </Grid>
</Page>
