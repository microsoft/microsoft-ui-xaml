<Page
    x:Class="Flick.FlexDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flick"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:utilities="using:MUXControlsTestApp.Utilities"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Resources>
            <local:Flex x:Name="layout"/>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel>
            <utilities:EntityPropertiesControl Entity="{StaticResource layout}" />
        </StackPanel>
        <ScrollViewer Grid.Column="1" Margin="10">
            <StackPanel>
                <TextBlock Text="Architecture" FontSize="28" Margin="10" />
                <controls:ItemsRepeater  Layout="{StaticResource layout}" x:Name="repeater">
                    <DataTemplate>
                        <UserControl Margin="5" IsTabStop="True" UseSystemFocusVisuals="True" Width="300" VerticalAlignment="Stretch">
                            <StackPanel>
                                <Image Source="{Binding UrlMedium}" Stretch="UniformToFill" />
                                <TextBlock Text="{Binding Description}" TextWrapping="WrapWholeWords" FontSize="16"/>
                            </StackPanel>
                        </UserControl>
                    </DataTemplate>
                </controls:ItemsRepeater>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
