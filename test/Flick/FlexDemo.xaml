<Page
    x:Class="Flick.FlexDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flick"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:utilities="using:MUXControlsTestApp.Utilities"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid KeyDown="Grid_KeyDown">
        <Grid.Resources>
            <local:Flex x:Name="layout" FlexWrap="Wrap"/>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel>
            <utilities:EntityPropertiesControl Entity="{StaticResource layout}" />
        </StackPanel>
        <ScrollViewer Grid.Column="1" Padding="16">
            <StackPanel>
                <TextBlock Text="Architecture" FontSize="28" Margin="10" />
                <controls:ItemsRepeater  Layout="{StaticResource layout}" x:Name="repeater" Background="LightGray">
                    <DataTemplate>
                        <UserControl IsTabStop="True" UseSystemFocusVisuals="True"
                                     Margin="5"
                                     local:Flex.FlexBasis="{Binding FlexBasis, Mode=OneWay}"
                                     local:Flex.FlexGrow="{Binding FlexGrow, Mode=OneWay}"
                                     local:Flex.FlexShrink="{Binding FlexShrink, Mode=OneWay}">
                            <Grid>
                                <!-- BorderBrush="Red" BorderThickness="1"/> -->
                                <StackPanel>
                                    <Image Source="{Binding UrlMedium}" Stretch="UniformToFill" />
                                    <TextBlock Text="{Binding Description}" TextWrapping="WrapWholeWords" FontSize="16"/>
                                </StackPanel>
                                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20">
                                    <TextBox Text="{Binding FlexBasis, Mode=TwoWay}" Width="50" TextChanged="TextBox_TextChanged" />
                                    <TextBox Text="{Binding FlexGrow, Mode=TwoWay}" Width="50" TextChanged="TextBox_TextChanged" />
                                    <TextBox Text="{Binding FlexShrink, Mode=TwoWay}" Width="50" TextChanged="TextBox_TextChanged" />
                                </StackPanel>
                            </Grid>
                        </UserControl>
                    </DataTemplate>
                </controls:ItemsRepeater>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
