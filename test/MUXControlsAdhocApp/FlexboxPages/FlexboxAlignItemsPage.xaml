<Page
    x:Class="MUXControlsAdhocApp.FlexboxPages.FlexboxAlignItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsAdhocApp.FlexboxPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="Grid">
                <Setter Property="Background" Value="Orange"/>
                <Setter Property="MinWidth" Value="50"/>
                <Setter Property="MinHeight" Value="50"/>
                <Setter Property="Margin" Value="6"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="BorderBrush" Value="Black"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Style>
            <Style TargetType="local:Flexbox">
                <Setter Property="Background" Value="Purple"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
            </Style>
            <Style x:Key="HeaderStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
            </Style>
        </StackPanel.Resources>

        <TextBlock Style="{StaticResource HeaderStyle}">Start</TextBlock>

        <local:Flexbox Direction="Row" AlignItems="Start">
            <Grid MinHeight="100"/>
            <Grid MinHeight="120"/>
            <Grid MinHeight="60"/>
            <Grid MinHeight="100"/>
        </local:Flexbox>

        <TextBlock Style="{StaticResource HeaderStyle}">End</TextBlock>

        <local:Flexbox Direction="Row" AlignItems="End">
            <Grid MinHeight="100"/>
            <Grid MinHeight="120"/>
            <Grid MinHeight="60"/>
            <Grid MinHeight="100"/>
        </local:Flexbox>

        <TextBlock Style="{StaticResource HeaderStyle}">Center</TextBlock>

        <local:Flexbox Direction="Row" AlignItems="Center">
            <Grid MinHeight="100"/>
            <Grid MinHeight="120"/>
            <Grid MinHeight="60"/>
            <Grid MinHeight="100"/>
        </local:Flexbox>

        <TextBlock Style="{StaticResource HeaderStyle}">Stretch</TextBlock>

        <local:Flexbox Direction="Row" AlignItems="Stretch">
            <Grid MinHeight="100"/>
            <Grid MinHeight="120"/>
            <Grid MinHeight="60"/>
            <Grid MinHeight="100"/>
        </local:Flexbox>

        <TextBlock Style="{StaticResource HeaderStyle}">Baseline</TextBlock>

        <local:Flexbox Direction="Row" AlignItems="Baseline">
            <local:Flexbox.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,20,0,0"/>
                </Style>
            </local:Flexbox.Resources>
            <Grid MinHeight="100" MinWidth="80">
                <TextBlock FontSize="15">text text</TextBlock>
            </Grid>
            <Grid MinHeight="120" MinWidth="80">
                <TextBlock FontSize="12">text text</TextBlock>
            </Grid>
            <Grid MinHeight="60" MinWidth="80">
                <TextBlock FontSize="18">text text</TextBlock>
            </Grid>
            <Grid MinHeight="100" MinWidth="80">
                <TextBlock FontSize="15">text text</TextBlock>
            </Grid>
        </local:Flexbox>
    </StackPanel>
</Page>
