<Page
    x:Class="SwitcherPrototype.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SwitcherPrototype"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="10" Spacing="10">
        <local:ViewSwitcher x:Name="ViewSwitcher" SelectedIndex="1" ShowPillVisual="{Binding ElementName=showPill, Path=IsOn}">
            <local:ViewSwitcher.ItemsSource>
                <local:MyCollection>
                    <x:String>Option 0</x:String>
                    <x:String>Option 1</x:String>
                    <x:String>Option 2</x:String>
                </local:MyCollection>
            </local:ViewSwitcher.ItemsSource>
        </local:ViewSwitcher>

        <TextBlock>Selected Index:</TextBlock>
        <TextBlock Text="{Binding ElementName=ViewSwitcher, Path=SelectedIndex}" />

        <TextBlock>Selected Item:</TextBlock>
        <TextBlock Text="{Binding ElementName=ViewSwitcher, Path=SelectedItem}" />

        <ToggleSwitch Header="Show Pill" x:Name="showPill" />

    </StackPanel>
</Page>
