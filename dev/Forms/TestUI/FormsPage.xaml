<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<local:TestPage
    x:Class="MUXControlsTestApp.FormsPage"
    x:Name="FormsTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsTestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--<controls:RecyclePool x:Key="RecyclePool" />
        <controls:RecyclingElementFactory x:Name="elementFactory"
                            RecyclePool="{StaticResource RecyclePool}">
            <DataTemplate x:Key="Item">
                <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="{Binding}" />
                </Button>
            </DataTemplate>
        </controls:RecyclingElementFactory>-->

        <local:FormsElementFactory x:Name="elementFactory"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="12">
        <StackPanel Orientation="Vertical">
            <!--<controls:Forms />-->

            <controls:FormSection Header="Section One" Columns="2">
                <TextBox Header="First Name"/>

                <TextBox Header="Last Name"/>

                <TextBox Header="M.I." controls:FormSection.Buddies="1" controls:FormSection.Length="Auto"/>
                <TextBox Header="Phone Number"/>

                <TextBox Header="Title" controls:FormSection.Buddies="1"/>
                <TextBox Header="Nickname"/>

                <TextBox Header="Something" controls:FormSection.Buddies="2"/>
                <TextBox Header="One" controls:FormSection.Length="Auto"/>
                <TextBox Header="Two" controls:FormSection.Length="100"/>
            </controls:FormSection>

            <!--<controls:ItemsRepeater x:Name="Repeater"/>-->
        </StackPanel>
    </Grid>
</local:TestPage>
