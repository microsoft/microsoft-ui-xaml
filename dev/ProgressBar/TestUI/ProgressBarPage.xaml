<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<local:TestPage
    x:Class="MUXControlsTestApp.ProgressBarPage"
    x:Name="ProgressBarTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsTestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="12">
        <StackPanel Orientation="Vertical">
            <controls:ProgressBar
                x:Name="TestProgressBar"
                AutomationProperties.Name="TestProgressBar"
                Maximum="10"
                Width="300"
                HorizontalAlignment="Left"
                IsIndeterminate="True"
                ShowPaused="False"
                ShowError="False"/>
            <TextBlock Text="{Binding ElementName=TestProgressBar, Path=Value, Mode=TwoWay}"/>
            <Button x:Name="ChangeValueButton" AutomationProperties.Name="ChangeValueButton" Content="Change Value" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</local:TestPage>
