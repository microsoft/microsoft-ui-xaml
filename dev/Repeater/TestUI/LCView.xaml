<UserControl
    x:Class="MUXControlsTestApp.Samples.LCView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Bind ViewModel.Text, Mode=OneWay}" />
        <ListView
            x:Name="SubItems"
            Grid.Row="1"
            x:Load="{x:Bind ViewModel.HasItems, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}" Height="2"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <!--<controls:ItemsRepeater
            x:Name="SubItems"
            Grid.Row="1"
            x:Load="{x:Bind ViewModel.HasItems, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
            <controls:ItemsRepeater.Layout>
                <controls:StackLayout Orientation="Vertical" DisableVirtualization="True"/>
            </controls:ItemsRepeater.Layout>
            <controls:ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}" />
                </DataTemplate>
            </controls:ItemsRepeater.ItemTemplate>
        </controls:ItemsRepeater>-->
    </Grid>
</UserControl>
