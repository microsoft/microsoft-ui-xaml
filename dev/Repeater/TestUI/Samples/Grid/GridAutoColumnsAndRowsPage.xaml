<Page
    x:Class="MUXControlsTestApp.Samples.GridAutoColumnsAndRowsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsTestApp.Samples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>

        <TextBlock>Columns: 120px 120px</TextBlock>
        <TextBlock>Rows: 180px 180px</TextBlock>
        <TextBlock>AutoColumns: 120px</TextBlock>

        <Grid>

            <controls:LayoutPanel>
                <controls:LayoutPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="18"/>
                    </Style>
                    <Style TargetType="Grid">
                        <Setter Property="Background" Value="Orange"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="CornerRadius" Value="6"/>
                        <Setter Property="BorderBrush" Value="Black"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                    </Style>
                </controls:LayoutPanel.Resources>
                <!--
                    grid-template-columns: 60px 60px;
                    grid-template-rows: 90px 90px
                    grid-auto-columns: 60px;
                -->
                <controls:LayoutPanel.Layout>
                    <controls:GridLayout>
                        <controls:GridLayout.TemplateColumns>
                            <controls:GridTrackInfo Length="120"/>
                            <controls:GridTrackInfo Length="120"/>
                        </controls:GridLayout.TemplateColumns>
                        <controls:GridLayout.TemplateRows>
                            <controls:GridTrackInfo Length="180"/>
                            <controls:GridTrackInfo Length="180"/>
                        </controls:GridLayout.TemplateRows>
                        <controls:GridLayout.AutoColumns>
                            <controls:GridTrackInfo Length="120"/>
                        </controls:GridLayout.AutoColumns>    
                    </controls:GridLayout>
                </controls:LayoutPanel.Layout>

                <!--
                .item-a {
                  grid-column: 1 / 2;
                  grid-row: 2 / 3;
                }-->
                <Grid Background="#FFA500">
                    <controls:GridLayout.ColumnStart>
                        <controls:GridLocation Index="0"/>
                    </controls:GridLayout.ColumnStart>
                    <controls:GridLayout.ColumnEnd>
                        <controls:GridLocation Index="1"/>
                    </controls:GridLayout.ColumnEnd>
                    <controls:GridLayout.RowStart>
                        <controls:GridLocation Index="1"/>
                    </controls:GridLayout.RowStart>
                    <controls:GridLayout.RowEnd>
                        <controls:GridLocation Index="2"/>
                    </controls:GridLayout.RowEnd>
                    <TextBlock>item-a</TextBlock>
                </Grid>

                <!--
                .item-b {
                  grid-column: 5 / 6;
                  grid-row: 2 / 3;
                }-->
                <Grid Background="#0000FF">
                    <controls:GridLayout.ColumnStart>
                        <controls:GridLocation Index="4"/>
                    </controls:GridLayout.ColumnStart>
                    <controls:GridLayout.ColumnEnd>
                        <controls:GridLocation Index="5"/>
                    </controls:GridLayout.ColumnEnd>
                    <controls:GridLayout.RowStart>
                        <controls:GridLocation Index="1"/>
                    </controls:GridLayout.RowStart>
                    <controls:GridLayout.RowEnd>
                        <controls:GridLocation Index="2"/>
                    </controls:GridLayout.RowEnd>
                    <TextBlock>item-b</TextBlock>
                </Grid>

            </controls:LayoutPanel>

        </Grid>

    </StackPanel>
</Page>
