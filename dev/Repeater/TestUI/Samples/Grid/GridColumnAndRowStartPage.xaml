<Page
    x:Class="MUXControlsTestApp.Samples.GridColumnAndRowStartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsTestApp.Samples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>

        <TextBlock>Columns: 40px 50px auto 50px 40px</TextBlock>
        <TextBlock>Rows: 25% 100px auto</TextBlock>

        <!-- MaxHeight is necessary because 25% of Infinity is Infinity :/ -->
        <Grid MaxHeight="500">

            <controls:LayoutPanel>
                <controls:LayoutPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="18"/>
                    </Style>
                    <Style TargetType="Grid">
                        <Setter Property="Background" Value="Orange"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="CornerRadius" Value="6"/>
                        <Setter Property="BorderBrush" Value="Black"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                    </Style>
                </controls:LayoutPanel.Resources>

                <controls:LayoutPanel.Layout>
                    <controls:GridLayout>
                    
                        <!--
                            grid-template-columns: [first] 40px [line2] 50px [line3] auto [col4-start] 50px [five] 40px [end];
                            grid-template-rows: [row1-start] 25% [row1-end] 100px [third-line] auto [last-line];
                        -->
                        <controls:GridLayout.TemplateColumns>
                            <controls:GridTrackInfo Length="40" LineName="first"/>
                            <controls:GridTrackInfo Length="50" LineName="line2"/>
                            <controls:GridTrackInfo Auto="True" LineName="line3"/>
                            <controls:GridTrackInfo Length="50" LineName="col4-start"/>
                            <controls:GridTrackInfo Length="40" LineName="five"/>
                            <!-- TODO: LineName="end" as a name for the last edge -->
                        </controls:GridLayout.TemplateColumns>
                        <controls:GridLayout.TemplateRows>
                            <controls:GridTrackInfo Percentage="0.25" LineName="row1-start"/>
                            <controls:GridTrackInfo Length="100" LineName="row1-end"/>
                            <controls:GridTrackInfo Auto="True" LineName="third-line"/>
                            <!-- TODO: LineName="last-line" as a name for the last edge -->
                        </controls:GridLayout.TemplateRows>

                    </controls:GridLayout>
                </controls:LayoutPanel.Layout>

                <!--
                .item-a {
                    grid-column-start: 2;
                    grid-column-end: five;
                    grid-row-start: row1-start
                    grid-row-end: 3;
                }-->
                <Grid Background="#88FFA500">
                    <controls:GridLayout.ColumnStart>
                        <controls:GridLocation Index="1"/>
                    </controls:GridLayout.ColumnStart>
                    <controls:GridLayout.ColumnEnd>
                        <controls:GridLocation LineName="five"/>
                    </controls:GridLayout.ColumnEnd>
                    <controls:GridLayout.RowStart>
                        <controls:GridLocation LineName="row1-start"/>
                    </controls:GridLayout.RowStart>
                    <controls:GridLayout.RowEnd>
                        <controls:GridLocation Index="2"/>
                    </controls:GridLayout.RowEnd>
                    <TextBlock>item-a</TextBlock>
                </Grid>

                <!--
                .item-b {
                    grid-column-start: 1;
                    grid-column-end: span col4-start;
                    grid-row-start: 2
                    grid-row-end: span 2
                }-->
                <Grid Background="#880000FF">
                    <controls:GridLayout.ColumnStart>
                        <controls:GridLocation Index="0"/>
                    </controls:GridLayout.ColumnStart>
                    <controls:GridLayout.ColumnEnd>
                        <controls:GridLocation LineName="col4-start"/>
                    </controls:GridLayout.ColumnEnd>
                    <controls:GridLayout.RowStart>
                        <controls:GridLocation Index="1"/>
                    </controls:GridLayout.RowStart>
                    <controls:GridLayout.RowEnd>
                        <controls:GridLocation Span="2"/>
                    </controls:GridLayout.RowEnd>
                    <TextBlock>item-b</TextBlock>
                </Grid>

                <!--
                .item-c {
                    grid-column: 3 / span 2;
                    grid-row: third-line / 4;
                }-->
                <Grid Background="#88FF0000">
                    <controls:GridLayout.ColumnStart>
                        <controls:GridLocation Index="2"/>
                    </controls:GridLayout.ColumnStart>
                    <controls:GridLayout.ColumnEnd>
                        <controls:GridLocation Span="2"/>
                    </controls:GridLayout.ColumnEnd>
                    <controls:GridLayout.RowStart>
                        <controls:GridLocation LineName="third-line"/>
                    </controls:GridLayout.RowStart>
                    <controls:GridLayout.RowEnd>
                        <controls:GridLocation Index="3"/>
                    </controls:GridLayout.RowEnd>
                    <TextBlock>item-c</TextBlock>
                </Grid>

            </controls:LayoutPanel>

        </Grid>

    </StackPanel>
</Page>
