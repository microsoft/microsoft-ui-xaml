<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<StackPanel
    xmlns='http://schemas.microsoft.com/winfx/2006/xaml/presentation' 
    xmlns:x='http://schemas.microsoft.com/winfx/2006/xaml'>
    <StackPanel.Resources>
        <DataTemplate x:Key="ListItemTemplate">
            <Border x:Name="Item" Background="Red" MinWidth="100" MinHeight="50">
                <Grid RowDefinitions="50, Auto">
                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Rectangle Grid.Row="1" Height="100" Width="200" Fill="Green" Visibility="{Binding IsExpanded}"/>
                </Grid>
            </Border>
        </DataTemplate>
      
        <ItemsPanelTemplate x:Key="MaintainOffsetPanel">
            <ItemsStackPanel Background="Yellow" ItemsUpdatingScrollMode="KeepScrollOffset" CacheLength="0"/>
        </ItemsPanelTemplate>
    </StackPanel.Resources>

    <Grid x:Name="Root" Width="480" Height="700" Margin="32">
        <ListView x:Name="lv" Background="Gray"
                  ScrollViewer.HorizontalScrollMode="Auto" ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                  ScrollViewer.VerticalScrollMode="Auto" ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  ItemTemplate="{StaticResource ListItemTemplate}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel Background="Yellow"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="Green" Width="50" Height="50">
                                <TextBlock Text="{Binding Header}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</StackPanel>