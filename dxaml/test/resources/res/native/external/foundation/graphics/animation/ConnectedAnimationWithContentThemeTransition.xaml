<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<StackPanel xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            Orientation="Horizontal">
    <StackPanel.Resources>
        <Style x:Key="StackPanelTransitionStyle" TargetType="StackPanel">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style x:Key="RectangleStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </StackPanel.Resources>
    <StackPanel Style="{StaticResource StackPanelTransitionStyle}">
        <Rectangle x:Name="source" Style="{StaticResource RectangleStyle}" Fill="Red"/>
    </StackPanel>
    <StackPanel Style="{StaticResource StackPanelTransitionStyle}" x:Name="parent">
        <StackPanel.Transitions>
            <TransitionCollection>
                <ContentThemeTransition VerticalOffset="0" HorizontalOffset="0"/>
            </TransitionCollection>
        </StackPanel.Transitions>
        <Rectangle x:Name="destination" Style="{StaticResource RectangleStyle}" Fill="Orange"/>
    </StackPanel>
</StackPanel>
