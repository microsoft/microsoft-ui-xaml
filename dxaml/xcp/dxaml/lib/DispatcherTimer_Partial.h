// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.

//  Abstract:
//      DispatherTimer class is not generated by the ClassGen tool, it is now a regular object instead of DependencyObject.
//      But it still wants to interop with its native peer DispatcherTimer object, so some interop mechanism is implemented 
//      in this class.

#pragma once

#include "DispatcherTimer.g.h"

namespace DirectUI
{
    PARTIAL_CLASS(DispatcherTimer)
    {
    public:
        DispatcherTimer() : m_fIsEnabled(FALSE)
        {}

        _Check_return_ HRESULT get_IsEnabledImpl(_Out_ BOOLEAN* pValue);

        _Check_return_ HRESULT StartImpl();
        _Check_return_ HRESULT StopImpl();

    private:
        BOOLEAN m_fIsEnabled;
    };
}
