// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#pragma once

#include "StateTriggerBase.g.h"

#define __StateTrigger_GUID "cbc3a15e-2ec8-4334-80cc-8b488805ff4a"

namespace DirectUI
{
    class StateTrigger;

    class __declspec(novtable) __declspec(uuid(__StateTrigger_GUID)) StateTrigger:
        public DirectUI::StateTriggerBase
        , public ABI::Microsoft::UI::Xaml::IStateTrigger
    {

        INSPECTABLE_CLASS(L"Microsoft.UI.Xaml.StateTrigger");

        BEGIN_INTERFACE_MAP(StateTrigger, DirectUI::StateTriggerBase)
            INTERFACE_ENTRY(StateTrigger, ABI::Microsoft::UI::Xaml::IStateTrigger)
        END_INTERFACE_MAP(StateTrigger, DirectUI::StateTriggerBase)

    public:
        StateTrigger();
        ~StateTrigger() override;

        // Event source typedefs.

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::StateTrigger;
        }

        static XCP_FORCEINLINE KnownTypeIndex GetTypeIndexStatic()
        {
            return KnownTypeIndex::StateTrigger;
        }

        // Properties.
        IFACEMETHOD(get_IsActive)(_Out_ BOOLEAN* pValue) override;
        IFACEMETHOD(put_IsActive)(_In_ BOOLEAN value) override;

        // Events.

        // Methods.


    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

    private:
        // Customized properties.

        // Customized methods.

        // Fields.
    };
}


namespace DirectUI
{
    // Note that the ordering of the base types here is important - the base factory comes first, followed by all the
    // interfaces specific to this type.  By doing this, we allow every Factory's CreateInstance method to be more
    // COMDAT-folding-friendly.  Because this ensures that the first vfptr contains GetTypeIndex, it means that all
    // CreateInstance functions with the same base factory generate the same assembly instructions and thus will
    // fold together.  This is significant for binary size in Microsoft.UI.Xaml.dll so change this only with great
    // care.
    class __declspec(novtable) StateTriggerFactory:
       public ctl::BetterCoreObjectActivationFactory
        , public ABI::Microsoft::UI::Xaml::IStateTriggerStatics
    {
        BEGIN_INTERFACE_MAP(StateTriggerFactory, ctl::BetterCoreObjectActivationFactory)
            INTERFACE_ENTRY(StateTriggerFactory, ABI::Microsoft::UI::Xaml::IStateTriggerStatics)
        END_INTERFACE_MAP(StateTriggerFactory, ctl::BetterCoreObjectActivationFactory)

    public:
        // Factory methods.

        // Static properties.

        // Dependency properties.
        IFACEMETHOD(get_IsActiveProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;

        // Attached properties.

        // Static methods.

        // Static events.

    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::StateTrigger;
        }


    private:

        // Customized static properties.

        // Customized static  methods.
    };
}
