// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#include "IconSourceElement.g.h"
#include "IconSource.g.h"

// Constructors/destructors.
DirectUI::IconSourceElement::IconSourceElement()
{
}

DirectUI::IconSourceElement::~IconSourceElement()
{
}

HRESULT DirectUI::IconSourceElement::QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject)
{
    if (InlineIsEqualGUID(iid, __uuidof(DirectUI::IconSourceElement)))
    {
        *ppObject = static_cast<DirectUI::IconSourceElement*>(this);
    }
    else if (InlineIsEqualGUID(iid, __uuidof(ABI::Microsoft::UI::Xaml::Controls::IIconSourceElement)))
    {
        *ppObject = static_cast<ABI::Microsoft::UI::Xaml::Controls::IIconSourceElement*>(this);
    }
    else
    {
        RRETURN(DirectUI::IconElement::QueryInterfaceImpl(iid, ppObject));
    }

    AddRefOuter();
    RRETURN(S_OK);
}

// Properties.
IFACEMETHODIMP DirectUI::IconSourceElement::get_IconSource(_Outptr_result_maybenull_ ABI::Microsoft::UI::Xaml::Controls::IIconSource** ppValue)
{
    RRETURN(GetValueByKnownIndex(KnownPropertyIndex::IconSourceElement_IconSource, ppValue));
}
IFACEMETHODIMP DirectUI::IconSourceElement::put_IconSource(_In_opt_ ABI::Microsoft::UI::Xaml::Controls::IIconSource* pValue)
{
    IFC_RETURN(DefaultStrictApiCheck(this));
    RRETURN(SetValueByKnownIndex(KnownPropertyIndex::IconSourceElement_IconSource, pValue));
}

// Events.

// Methods.

HRESULT DirectUI::IconSourceElementFactory::QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject)
{
    if (InlineIsEqualGUID(iid, __uuidof(ABI::Microsoft::UI::Xaml::Controls::IIconSourceElementFactory)))
    {
        *ppObject = static_cast<ABI::Microsoft::UI::Xaml::Controls::IIconSourceElementFactory*>(this);
    }
    else if (InlineIsEqualGUID(iid, __uuidof(ABI::Microsoft::UI::Xaml::Controls::IIconSourceElementStatics)))
    {
        *ppObject = static_cast<ABI::Microsoft::UI::Xaml::Controls::IIconSourceElementStatics*>(this);
    }
    else
    {
        RRETURN(ctl::BetterAggregableCoreObjectActivationFactory::QueryInterfaceImpl(iid, ppObject));
    }

    AddRefOuter();
    RRETURN(S_OK);
}


// Factory methods.
IFACEMETHODIMP DirectUI::IconSourceElementFactory::CreateInstance(_In_opt_ IInspectable* pOuter, _Outptr_ IInspectable** ppInner, _Outptr_ ABI::Microsoft::UI::Xaml::Controls::IIconSourceElement** ppInstance)
{

#if DBG
    // We play some games with reinterpret_cast and assuming that the GUID type table is accurate - which is somewhat sketchy, but
    // really good for binary size.  This code is a sanity check that the games we play are ok.
    const GUID uuidofGUID = __uuidof(ABI::Microsoft::UI::Xaml::Controls::IIconSourceElement);
    const GUID metadataAPIGUID = MetadataAPI::GetClassInfoByIndex(GetTypeIndex())->GetGuid();
    const KnownTypeIndex typeIndex = GetTypeIndex();

    if(uuidofGUID != metadataAPIGUID)
    {
        XAML_FAIL_FAST();
    }
#endif

    // Can't just IFC(_RETURN) this because for some validate calls (those with multiple template parameters), the
    // preprocessor gets confused at the "," in the template type-list before the function's opening parenthesis.
    // So we'll use IFC_RETURN syntax with a local hr variable, kind of weirdly.
    const HRESULT hr = ctl::ValidateFactoryCreateInstanceWithBetterAggregableCoreObjectActivationFactory(pOuter, ppInner, reinterpret_cast<IUnknown**>(ppInstance), GetTypeIndex(), false /*isFreeThreaded*/);
    IFC_RETURN(hr);
    return S_OK;
}

// Dependency properties.
IFACEMETHODIMP DirectUI::IconSourceElementFactory::get_IconSourceProperty(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue)
{
    RRETURN(MetadataAPI::GetIDependencyProperty(KnownPropertyIndex::IconSourceElement_IconSource, ppValue));
}

// Attached properties.

// Static properties.

// Static methods.

namespace DirectUI
{
    _Check_return_ IActivationFactory* CreateActivationFactory_IconSourceElement()
    {
        RRETURN(ctl::ActivationFactoryCreator<IconSourceElementFactory>::CreateActivationFactory());
    }
}
