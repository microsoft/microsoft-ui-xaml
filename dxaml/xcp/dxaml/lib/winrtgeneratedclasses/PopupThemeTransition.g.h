// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#pragma once

#include "Transition.g.h"

#define __PopupThemeTransition_GUID "ce2d8cb3-39fa-4c56-b88a-f0aa1f7ba963"

namespace DirectUI
{
    class PopupThemeTransition;

    class __declspec(novtable) PopupThemeTransitionGenerated:
        public DirectUI::Transition
        , public ABI::Microsoft::UI::Xaml::Media::Animation::IPopupThemeTransition
    {
        friend class DirectUI::PopupThemeTransition;

        INSPECTABLE_CLASS(L"Microsoft.UI.Xaml.Media.Animation.PopupThemeTransition");

        BEGIN_INTERFACE_MAP(PopupThemeTransitionGenerated, DirectUI::Transition)
            INTERFACE_ENTRY(PopupThemeTransitionGenerated, ABI::Microsoft::UI::Xaml::Media::Animation::IPopupThemeTransition)
        END_INTERFACE_MAP(PopupThemeTransitionGenerated, DirectUI::Transition)

    public:
        PopupThemeTransitionGenerated();
        ~PopupThemeTransitionGenerated() override;

        // Event source typedefs.

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::PopupThemeTransition;
        }

        static XCP_FORCEINLINE KnownTypeIndex GetTypeIndexStatic()
        {
            return KnownTypeIndex::PopupThemeTransition;
        }

        // Properties.
        IFACEMETHOD(get_FromHorizontalOffset)(_Out_ DOUBLE* pValue) override;
        IFACEMETHOD(put_FromHorizontalOffset)(_In_ DOUBLE value) override;
        IFACEMETHOD(get_FromVerticalOffset)(_Out_ DOUBLE* pValue) override;
        IFACEMETHOD(put_FromVerticalOffset)(_In_ DOUBLE value) override;

        // Events.

        // Methods.


    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

    private:

        // Fields.
    };
}

#include "PopupThemeTransition_Partial.h"

namespace DirectUI
{
    // Note that the ordering of the base types here is important - the base factory comes first, followed by all the
    // interfaces specific to this type.  By doing this, we allow every Factory's CreateInstance method to be more
    // COMDAT-folding-friendly.  Because this ensures that the first vfptr contains GetTypeIndex, it means that all
    // CreateInstance functions with the same base factory generate the same assembly instructions and thus will
    // fold together.  This is significant for binary size in Microsoft.UI.Xaml.dll so change this only with great
    // care.
    class __declspec(novtable) PopupThemeTransitionFactory:
       public ctl::BetterCoreObjectActivationFactory
        , public ABI::Microsoft::UI::Xaml::Media::Animation::IPopupThemeTransitionStatics
    {
        BEGIN_INTERFACE_MAP(PopupThemeTransitionFactory, ctl::BetterCoreObjectActivationFactory)
            INTERFACE_ENTRY(PopupThemeTransitionFactory, ABI::Microsoft::UI::Xaml::Media::Animation::IPopupThemeTransitionStatics)
        END_INTERFACE_MAP(PopupThemeTransitionFactory, ctl::BetterCoreObjectActivationFactory)

    public:
        // Factory methods.

        // Static properties.

        // Dependency properties.
        IFACEMETHOD(get_FromHorizontalOffsetProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;
        IFACEMETHOD(get_FromVerticalOffsetProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;

        // Attached properties.

        // Static methods.

        // Static events.

    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::PopupThemeTransition;
        }


    private:

        // Customized static properties.

        // Customized static  methods.
    };
}
