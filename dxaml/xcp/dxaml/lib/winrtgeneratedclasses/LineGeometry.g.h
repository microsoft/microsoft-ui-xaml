// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#pragma once

#include "Geometry.g.h"

#define __LineGeometry_GUID "57f2b55e-69da-439e-a01f-d0a130acce66"

namespace DirectUI
{
    class LineGeometry;

    class __declspec(novtable) __declspec(uuid(__LineGeometry_GUID)) LineGeometry:
        public DirectUI::Geometry
        , public ABI::Microsoft::UI::Xaml::Media::ILineGeometry
    {

        INSPECTABLE_CLASS(L"Microsoft.UI.Xaml.Media.LineGeometry");

        BEGIN_INTERFACE_MAP(LineGeometry, DirectUI::Geometry)
            INTERFACE_ENTRY(LineGeometry, ABI::Microsoft::UI::Xaml::Media::ILineGeometry)
        END_INTERFACE_MAP(LineGeometry, DirectUI::Geometry)

    public:
        LineGeometry();
        ~LineGeometry() override;

        // Event source typedefs.

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::LineGeometry;
        }

        static XCP_FORCEINLINE KnownTypeIndex GetTypeIndexStatic()
        {
            return KnownTypeIndex::LineGeometry;
        }

        // Properties.
        IFACEMETHOD(get_EndPoint)(_Out_ ABI::Windows::Foundation::Point* pValue) override;
        IFACEMETHOD(put_EndPoint)(_In_ ABI::Windows::Foundation::Point value) override;
        IFACEMETHOD(get_StartPoint)(_Out_ ABI::Windows::Foundation::Point* pValue) override;
        IFACEMETHOD(put_StartPoint)(_In_ ABI::Windows::Foundation::Point value) override;

        // Events.

        // Methods.


    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

    private:
        // Customized properties.

        // Customized methods.

        // Fields.
    };
}


namespace DirectUI
{
    // Note that the ordering of the base types here is important - the base factory comes first, followed by all the
    // interfaces specific to this type.  By doing this, we allow every Factory's CreateInstance method to be more
    // COMDAT-folding-friendly.  Because this ensures that the first vfptr contains GetTypeIndex, it means that all
    // CreateInstance functions with the same base factory generate the same assembly instructions and thus will
    // fold together.  This is significant for binary size in Microsoft.UI.Xaml.dll so change this only with great
    // care.
    class __declspec(novtable) LineGeometryFactory:
       public ctl::BetterCoreObjectActivationFactory
        , public ABI::Microsoft::UI::Xaml::Media::ILineGeometryStatics
    {
        BEGIN_INTERFACE_MAP(LineGeometryFactory, ctl::BetterCoreObjectActivationFactory)
            INTERFACE_ENTRY(LineGeometryFactory, ABI::Microsoft::UI::Xaml::Media::ILineGeometryStatics)
        END_INTERFACE_MAP(LineGeometryFactory, ctl::BetterCoreObjectActivationFactory)

    public:
        // Factory methods.

        // Static properties.

        // Dependency properties.
        IFACEMETHOD(get_StartPointProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;
        IFACEMETHOD(get_EndPointProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;

        // Attached properties.

        // Static methods.

        // Static events.

    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::LineGeometry;
        }


    private:

        // Customized static properties.

        // Customized static  methods.
    };
}
