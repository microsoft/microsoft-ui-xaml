// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#pragma once

#include "Transition.g.h"

#define __RepositionThemeTransition_GUID "3514516d-07a6-4695-acd5-4c84108088cd"

namespace DirectUI
{
    class RepositionThemeTransition;

    class __declspec(novtable) RepositionThemeTransitionGenerated:
        public DirectUI::Transition
        , public ABI::Microsoft::UI::Xaml::Media::Animation::IRepositionThemeTransition
    {
        friend class DirectUI::RepositionThemeTransition;

        INSPECTABLE_CLASS(L"Microsoft.UI.Xaml.Media.Animation.RepositionThemeTransition");

        BEGIN_INTERFACE_MAP(RepositionThemeTransitionGenerated, DirectUI::Transition)
            INTERFACE_ENTRY(RepositionThemeTransitionGenerated, ABI::Microsoft::UI::Xaml::Media::Animation::IRepositionThemeTransition)
        END_INTERFACE_MAP(RepositionThemeTransitionGenerated, DirectUI::Transition)

    public:
        RepositionThemeTransitionGenerated();
        ~RepositionThemeTransitionGenerated() override;

        // Event source typedefs.

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::RepositionThemeTransition;
        }

        static XCP_FORCEINLINE KnownTypeIndex GetTypeIndexStatic()
        {
            return KnownTypeIndex::RepositionThemeTransition;
        }

        // Properties.
        IFACEMETHOD(get_IsStaggeringEnabled)(_Out_ BOOLEAN* pValue) override;
        IFACEMETHOD(put_IsStaggeringEnabled)(_In_ BOOLEAN value) override;

        // Events.

        // Methods.


    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

    private:

        // Fields.
    };
}

#include "RepositionThemeTransition_Partial.h"

namespace DirectUI
{
    // Note that the ordering of the base types here is important - the base factory comes first, followed by all the
    // interfaces specific to this type.  By doing this, we allow every Factory's CreateInstance method to be more
    // COMDAT-folding-friendly.  Because this ensures that the first vfptr contains GetTypeIndex, it means that all
    // CreateInstance functions with the same base factory generate the same assembly instructions and thus will
    // fold together.  This is significant for binary size in Microsoft.UI.Xaml.dll so change this only with great
    // care.
    class __declspec(novtable) RepositionThemeTransitionFactory:
       public ctl::BetterCoreObjectActivationFactory
        , public ABI::Microsoft::UI::Xaml::Media::Animation::IRepositionThemeTransitionStatics
    {
        BEGIN_INTERFACE_MAP(RepositionThemeTransitionFactory, ctl::BetterCoreObjectActivationFactory)
            INTERFACE_ENTRY(RepositionThemeTransitionFactory, ABI::Microsoft::UI::Xaml::Media::Animation::IRepositionThemeTransitionStatics)
        END_INTERFACE_MAP(RepositionThemeTransitionFactory, ctl::BetterCoreObjectActivationFactory)

    public:
        // Factory methods.

        // Static properties.

        // Dependency properties.
        IFACEMETHOD(get_IsStaggeringEnabledProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;

        // Attached properties.

        // Static methods.

        // Static events.

    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::RepositionThemeTransition;
        }


    private:

        // Customized static properties.

        // Customized static  methods.
    };
}
