// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#pragma once

#include "Geometry.g.h"

#define __EllipseGeometry_GUID "f79c4329-b583-4f5c-8b32-62bfa0162e61"

namespace DirectUI
{
    class EllipseGeometry;

    class __declspec(novtable) __declspec(uuid(__EllipseGeometry_GUID)) EllipseGeometry:
        public DirectUI::Geometry
        , public ABI::Microsoft::UI::Xaml::Media::IEllipseGeometry
    {

        INSPECTABLE_CLASS(L"Microsoft.UI.Xaml.Media.EllipseGeometry");

        BEGIN_INTERFACE_MAP(EllipseGeometry, DirectUI::Geometry)
            INTERFACE_ENTRY(EllipseGeometry, ABI::Microsoft::UI::Xaml::Media::IEllipseGeometry)
        END_INTERFACE_MAP(EllipseGeometry, DirectUI::Geometry)

    public:
        EllipseGeometry();
        ~EllipseGeometry() override;

        // Event source typedefs.

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::EllipseGeometry;
        }

        static XCP_FORCEINLINE KnownTypeIndex GetTypeIndexStatic()
        {
            return KnownTypeIndex::EllipseGeometry;
        }

        // Properties.
        IFACEMETHOD(get_Center)(_Out_ ABI::Windows::Foundation::Point* pValue) override;
        IFACEMETHOD(put_Center)(_In_ ABI::Windows::Foundation::Point value) override;
        IFACEMETHOD(get_RadiusX)(_Out_ DOUBLE* pValue) override;
        IFACEMETHOD(put_RadiusX)(_In_ DOUBLE value) override;
        IFACEMETHOD(get_RadiusY)(_Out_ DOUBLE* pValue) override;
        IFACEMETHOD(put_RadiusY)(_In_ DOUBLE value) override;

        // Events.

        // Methods.


    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

    private:
        // Customized properties.

        // Customized methods.

        // Fields.
    };
}


namespace DirectUI
{
    // Note that the ordering of the base types here is important - the base factory comes first, followed by all the
    // interfaces specific to this type.  By doing this, we allow every Factory's CreateInstance method to be more
    // COMDAT-folding-friendly.  Because this ensures that the first vfptr contains GetTypeIndex, it means that all
    // CreateInstance functions with the same base factory generate the same assembly instructions and thus will
    // fold together.  This is significant for binary size in Microsoft.UI.Xaml.dll so change this only with great
    // care.
    class __declspec(novtable) EllipseGeometryFactory:
       public ctl::BetterCoreObjectActivationFactory
        , public ABI::Microsoft::UI::Xaml::Media::IEllipseGeometryStatics
    {
        BEGIN_INTERFACE_MAP(EllipseGeometryFactory, ctl::BetterCoreObjectActivationFactory)
            INTERFACE_ENTRY(EllipseGeometryFactory, ABI::Microsoft::UI::Xaml::Media::IEllipseGeometryStatics)
        END_INTERFACE_MAP(EllipseGeometryFactory, ctl::BetterCoreObjectActivationFactory)

    public:
        // Factory methods.

        // Static properties.

        // Dependency properties.
        IFACEMETHOD(get_CenterProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;
        IFACEMETHOD(get_RadiusXProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;
        IFACEMETHOD(get_RadiusYProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;

        // Attached properties.

        // Static methods.

        // Static events.

    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::EllipseGeometry;
        }


    private:

        // Customized static properties.

        // Customized static  methods.
    };
}
