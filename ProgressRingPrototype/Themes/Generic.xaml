<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProgressRingPrototype">

    <Style TargetType="local:ProgressUI" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ProgressUI">
                    <Grid
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Indeterminate">
                                    <VisualState.Setters>
                                        <Setter Target="player.Source">
                                            <Setter.Value>
                                                <!--<local:WindowsLogoAnimatedVisual
                                                    Foreground="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TemplateSettings.ForegroundColor}"/>-->
                                                <local:IndeterminateRing
                                                    Background="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TemplateSettings.BackgroundColor}"
                                                    Foreground="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TemplateSettings.ForegroundColor}"                                                    
                                                    StrokeWidth="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=StrokeWidth}"
                                                    />
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Target="player.IsLooping" Value="True" />
                                        <Setter Target="player.IsPlaying" Value="True" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <local:AnimatedVisualPlayerProposed 
                            x:Name="player" 
                            Position="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TemplateSettings.ProgressPosition}">
                            <local:AnimatedVisualPlayerProposed.Source>
                                <local:LoadAnimation />
                            </local:AnimatedVisualPlayerProposed.Source>
                        </local:AnimatedVisualPlayerProposed>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    
</ResourceDictionary>
