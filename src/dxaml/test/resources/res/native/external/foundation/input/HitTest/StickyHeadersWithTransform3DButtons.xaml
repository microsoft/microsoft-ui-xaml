<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      x:Name="rootGrid"
      Background="Black"
      Margin="50" >
    <Grid.Resources>
        <CollectionViewSource x:Name="cvs" IsSourceGrouped="True"/>
        <Style TargetType="ListViewItem" x:Key="MyListViewItemStyle">
            <Setter Property="Margin" Value="0,0,0,0" />
        </Style>
    </Grid.Resources>
    <Grid x:Name="innerGrid" Width="200" Height="500" HorizontalAlignment="Left" VerticalAlignment="Top">
        <ListView x:Name="listView" ItemsSource="{Binding Source={StaticResource cvs}}" ItemContainerStyle="{StaticResource MyListViewItemStyle}" SelectionMode="None">
            <ListView.Resources>
              <x:Double x:Key='ListViewItemMinHeight'>44</x:Double>
            </ListView.Resources>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel CacheLength="0" AreStickyGroupHeadersEnabled="True"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Button Width="200" Height="50" Background="Gray" Margin="-12,-8,-12,0" Content="{Binding}">
                                <Button.Transform3D>
                                    <CompositeTransform3D RotationY="30" />
                                </Button.Transform3D>
                            </Button>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Grid>