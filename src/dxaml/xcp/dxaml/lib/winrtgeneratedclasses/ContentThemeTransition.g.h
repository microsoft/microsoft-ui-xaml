// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
//------------------------------------------------------------------------
//
//  Abstract:
//
//      XAML types.
//      NOTE: This file was generated by a tool.
//
//------------------------------------------------------------------------

#pragma once

#include "Transition.g.h"

#define __ContentThemeTransition_GUID "ae44c375-6d13-4366-9239-10e647d048fb"

namespace DirectUI
{
    class ContentThemeTransition;

    class __declspec(novtable) ContentThemeTransitionGenerated:
        public DirectUI::Transition
        , public ABI::Microsoft::UI::Xaml::Media::Animation::IContentThemeTransition
    {
        friend class DirectUI::ContentThemeTransition;

        INSPECTABLE_CLASS(L"Microsoft.UI.Xaml.Media.Animation.ContentThemeTransition");

        BEGIN_INTERFACE_MAP(ContentThemeTransitionGenerated, DirectUI::Transition)
            INTERFACE_ENTRY(ContentThemeTransitionGenerated, ABI::Microsoft::UI::Xaml::Media::Animation::IContentThemeTransition)
        END_INTERFACE_MAP(ContentThemeTransitionGenerated, DirectUI::Transition)

    public:
        ContentThemeTransitionGenerated();
        ~ContentThemeTransitionGenerated() override;

        // Event source typedefs.

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::ContentThemeTransition;
        }

        static XCP_FORCEINLINE KnownTypeIndex GetTypeIndexStatic()
        {
            return KnownTypeIndex::ContentThemeTransition;
        }

        // Properties.
        IFACEMETHOD(get_HorizontalOffset)(_Out_ DOUBLE* pValue) override;
        IFACEMETHOD(put_HorizontalOffset)(_In_ DOUBLE value) override;
        IFACEMETHOD(get_VerticalOffset)(_Out_ DOUBLE* pValue) override;
        IFACEMETHOD(put_VerticalOffset)(_In_ DOUBLE value) override;

        // Events.

        // Methods.


    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

    private:

        // Fields.
    };
}

#include "ContentThemeTransition_Partial.h"

namespace DirectUI
{
    // Note that the ordering of the base types here is important - the base factory comes first, followed by all the
    // interfaces specific to this type.  By doing this, we allow every Factory's CreateInstance method to be more
    // COMDAT-folding-friendly.  Because this ensures that the first vfptr contains GetTypeIndex, it means that all
    // CreateInstance functions with the same base factory generate the same assembly instructions and thus will
    // fold together.  This is significant for binary size in Microsoft.UI.Xaml.dll so change this only with great
    // care.
    class __declspec(novtable) ContentThemeTransitionFactory:
       public ctl::BetterCoreObjectActivationFactory
        , public ABI::Microsoft::UI::Xaml::Media::Animation::IContentThemeTransitionStatics
    {
        BEGIN_INTERFACE_MAP(ContentThemeTransitionFactory, ctl::BetterCoreObjectActivationFactory)
            INTERFACE_ENTRY(ContentThemeTransitionFactory, ABI::Microsoft::UI::Xaml::Media::Animation::IContentThemeTransitionStatics)
        END_INTERFACE_MAP(ContentThemeTransitionFactory, ctl::BetterCoreObjectActivationFactory)

    public:
        // Factory methods.

        // Static properties.

        // Dependency properties.
        IFACEMETHOD(get_HorizontalOffsetProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;
        IFACEMETHOD(get_VerticalOffsetProperty)(_Out_ ABI::Microsoft::UI::Xaml::IDependencyProperty** ppValue) override;

        // Attached properties.

        // Static methods.

        // Static events.

    protected:
        HRESULT QueryInterfaceImpl(_In_ REFIID iid, _Outptr_ void** ppObject) override;

        KnownTypeIndex GetTypeIndex() const override
        {
            return KnownTypeIndex::ContentThemeTransition;
        }


    private:

        // Customized static properties.

        // Customized static  methods.
    };
}
