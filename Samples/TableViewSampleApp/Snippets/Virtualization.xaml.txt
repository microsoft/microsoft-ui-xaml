<!-- VirtualizationPage.xaml — bind a large source; the v1 TableView realizes
     only the rows in the viewport, so memory and scroll stay flat. -->
<adv:TableView x:Name="PeopleTable" SelectionMode="Single" MinHeight="320">
    <adv:TableViewTextColumn Header="#"          Binding="{Binding Id}"        CanUserSort="False"/>
    <adv:TableViewTextColumn Header="First name" Binding="{Binding FirstName}" CanUserSort="False"/>
    <adv:TableViewTextColumn Header="Email"      Binding="{Binding Email}"     CanUserSort="False"/>
</adv:TableView>
