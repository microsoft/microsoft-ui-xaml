<Page
    x:Class="SelectionModelSampleApp.Pages.RangeSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:SelectionModelSampleApp.Common"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Page.Resources>
        <common:SelectionStateToBrushConverter x:Key="StateToBrush" />
        <common:SelectionStateToGlyphConverter x:Key="StateToGlyph" />
    </Page.Resources>

    <Grid Padding="16" ColumnSpacing="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="480" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="2" Spacing="4">
            <TextBlock Text="Anchor and range selection" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock TextWrapping="Wrap"
                       Text="The anchor is the fixed end of a range - the equivalent of the item you clicked before shift-clicking. Select/Deselect move it; the range methods do not." />
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ScrollViewer Padding="8">
                <muxc:ItemsRepeater x:Name="Repeater">
                    <muxc:ItemsRepeater.Layout>
                        <muxc:UniformGridLayout MinItemWidth="88" MinItemHeight="72"
                                                MinRowSpacing="8" MinColumnSpacing="8" />
                    </muxc:ItemsRepeater.Layout>
                    <muxc:ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsSelected, Converter={StaticResource StateToBrush}}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4" Padding="8">
                                <Grid>
                                    <TextBlock Text="{Binding Label}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="14"
                                              Glyph="{Binding IsSelected, Converter={StaticResource StateToGlyph}}"
                                              HorizontalAlignment="Right" VerticalAlignment="Top" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </muxc:ItemsRepeater.ItemTemplate>
                </muxc:ItemsRepeater>
            </ScrollViewer>
        </Border>

        <ScrollViewer Grid.Row="1" Grid.Column="1">
            <StackPanel Spacing="8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <StackPanel>
                        <TextBlock Text="Start / anchor" />
                        <NumberBox x:Name="StartBox" Value="4" Minimum="0" Maximum="19" Width="110"
                                   SpinButtonPlacementMode="Inline" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="End" />
                        <NumberBox x:Name="EndBox" Value="9" Minimum="0" Maximum="19" Width="110"
                                   SpinButtonPlacementMode="Inline" />
                    </StackPanel>
                </StackPanel>

                <Button Content="SetAnchorIndex(start)" Click="OnSetAnchorIndex" HorizontalAlignment="Stretch" />
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="SelectRangeFromAnchor(end)" Click="OnSelectRangeFromAnchor" />
                    <Button Content="DeselectRangeFromAnchor(end)" Click="OnDeselectRangeFromAnchor" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="SelectRangeFromAnchorTo(R.end)" Click="OnSelectRangeFromAnchorTo" />
                    <Button Content="DeselectRangeFromAnchorTo(R.end)" Click="OnDeselectRangeFromAnchorTo" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="SelectRange(start,end)" Click="OnSelectRange" />
                    <Button Content="DeselectRange(start,end)" Click="OnDeselectRange" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="AnchorIndex = null" Click="OnClearAnchor" />
                    <Button Content="ClearSelection()" Click="OnClearSelection" />
                </StackPanel>

                <TextBlock x:Name="ResultText" TextWrapping="Wrap" FontFamily="Consolas"
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Model state" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="StateText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
