<Page
    x:Class="SelectionModelSampleApp.Pages.FlatSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:SelectionModelSampleApp.Common"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Page.Resources>
        <common:SelectionStateToBrushConverter x:Key="StateToBrush" />
        <common:SelectionStateToGlyphConverter x:Key="StateToGlyph" />
    </Page.Resources>

    <Grid Padding="16" ColumnSpacing="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="2" Spacing="4">
            <TextBlock Text="Flat selection" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock TextWrapping="Wrap"
                       Text="SelectionModel over a flat list. Click a tile to toggle it, or drive the model directly with the buttons on the right." />
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ScrollViewer Padding="8">
                <muxc:ItemsRepeater x:Name="Repeater" ElementPrepared="OnElementPrepared">
                    <muxc:ItemsRepeater.Layout>
                        <muxc:UniformGridLayout MinItemWidth="88" MinItemHeight="72"
                                                MinRowSpacing="8" MinColumnSpacing="8" />
                    </muxc:ItemsRepeater.Layout>
                    <muxc:ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsSelected, Converter={StaticResource StateToBrush}}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4" Padding="8">
                                <Grid>
                                    <TextBlock Text="{Binding Label}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              FontSize="14"
                                              Glyph="{Binding IsSelected, Converter={StaticResource StateToGlyph}}"
                                              HorizontalAlignment="Right" VerticalAlignment="Top" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </muxc:ItemsRepeater.ItemTemplate>
                </muxc:ItemsRepeater>
            </ScrollViewer>
        </Border>

        <ScrollViewer Grid.Row="1" Grid.Column="1">
            <StackPanel Spacing="8">
                <ToggleSwitch x:Name="SingleSelectSwitch" Header="SingleSelect"
                              OffContent="false" OnContent="true"
                              Toggled="OnSingleSelectToggled" />

                <TextBlock Text="Index" />
                <NumberBox x:Name="IndexBox" Value="3" Minimum="0" Maximum="19"
                           SpinButtonPlacementMode="Inline" />

                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="Select(i)" Click="OnSelect" />
                    <Button Content="Deselect(i)" Click="OnDeselect" />
                    <Button Content="IsSelected(i)" Click="OnIsSelected" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="SelectAll()" Click="OnSelectAll" />
                    <Button Content="SelectAllFlat()" Click="OnSelectAllFlat" />
                    <Button Content="ClearSelection()" Click="OnClearSelection" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="SelectedIndex = R.5" Click="OnSetSelectedIndex" />
                    <Button Content="Source = null" Click="OnClearSource" />
                </StackPanel>

                <TextBlock x:Name="ResultText" TextWrapping="Wrap"
                           FontFamily="Consolas"
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Model state" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="StateText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10">
                    <StackPanel>
                        <TextBlock Text="SelectionChanged log" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="EventLogText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
