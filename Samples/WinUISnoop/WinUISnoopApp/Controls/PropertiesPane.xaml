<UserControl
    x:Class="WinUISnoopApp.Controls.PropertiesPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:WinUISnoopApp.SnoopData"
    xmlns:ct="using:CommunityToolkit.WinUI.Controls"
    x:Name="Root"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Header="Filter by Property Name" Margin="4"
                 Text="{Binding PropertyFilterText, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox Header="Selected Property Name" IsReadOnly="True" Margin="4"
                     Text="{Binding SelectedPropertyName, ElementName=Root, Mode=OneWay}"/>
            <TextBox Header="Property Value" Grid.Column="1" Margin="4"
                Text="{Binding PropertyValueText, ElementName=Root, Mode=TwoWay}"
                KeyDown="PropertyValueTextBox_KeyDown" />
        </Grid>
        <ListView
            Grid.Row="2"
            Margin="0,8,0,0"
            Padding="4,0"
            Background="{ThemeResource ControlFillColorTertiaryBrush}"
            ItemsSource="{Binding DisplayedProperties, ElementName=Root}"
            SelectedItem="{Binding SelectedProperty, ElementName=Root, Mode=TwoWay}"
            SelectionMode="Single">
            <ListView.Header>
                <ct:DataTable Height="40">
                    <ct:DataColumn Content="Name" DesiredWidth="*" CanResize="True" />
                    <ct:DataColumn Content="Value" DesiredWidth="*" CanResize="True" />
                    <ct:DataColumn Content="Source" DesiredWidth="100" CanResize="True" />
                </ct:DataTable>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:ElementProperty">
                    <ct:DataRow Margin="16 0 0 0">
                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center" />
                        <TextBlock Text="{x:Bind Value, Mode=OneWay}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind Value, Mode=OneWay}" VerticalAlignment="Center" />
                        <TextBlock Text="{x:Bind Source, Mode=OneWay}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind Source, Mode=OneWay}" VerticalAlignment="Center" />
                    </ct:DataRow>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                    <Setter Property="MinHeight" Value="12" />
                    <Setter Property="Padding" Value="0,8" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
