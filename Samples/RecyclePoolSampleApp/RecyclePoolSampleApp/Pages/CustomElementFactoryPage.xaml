<Page
    x:Class="RecyclePoolSampleApp.Pages.CustomElementFactoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:RecyclePoolSampleApp.Common"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Page.Resources>
        <DataTemplate x:Key="HeaderTemplate">
            <Border Background="#2F5D8C" CornerRadius="4" Padding="10" Margin="0,0,0,4">
                <TextBlock Text="{Binding Label}" Foreground="White"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="ItemTemplate">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="4" Padding="10" Margin="16,0,0,4">
                <TextBlock Text="{Binding Label}" />
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="FooterTemplate">
            <Border Background="#2F7D5A" CornerRadius="4" Padding="6" Margin="0,0,0,10">
                <TextBlock Text="{Binding Label}" Foreground="White" FontSize="12" />
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="16" ColumnSpacing="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="420" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="2" Spacing="4">
            <TextBlock Text="Custom ElementFactory" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock TextWrapping="Wrap"
                       Text="ElementFactory is the base class you derive from when RecyclingElementFactory's one-DataTemplate-per-key model does not fit. Override GetElementCore and RecycleElementCore; the sealed GetElement / RecycleElement pair forwards to them. This factory owns its own RecyclePool and keeps its own element-to-key map." />
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ScrollViewer x:Name="Scroller" Padding="8">
                <muxc:ItemsRepeater x:Name="Repeater">
                    <muxc:ItemsRepeater.Layout>
                        <muxc:StackLayout />
                    </muxc:ItemsRepeater.Layout>
                </muxc:ItemsRepeater>
            </ScrollViewer>
        </Border>

        <ScrollViewer Grid.Row="1" Grid.Column="1">
            <StackPanel Spacing="8">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Button Content="Scroll down" Click="OnScrollDown" />
                    <Button Content="Scroll to top" Click="OnScrollTop" />
                    <Button Content="Reset counters" Click="OnResetCounters" />
                </StackPanel>

                <TextBlock x:Name="ResultText" TextWrapping="Wrap"
                           FontFamily="Consolas"
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Factory state" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="StateText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Pool contents" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="PoolText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Override dispatch" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="DispatchText" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
