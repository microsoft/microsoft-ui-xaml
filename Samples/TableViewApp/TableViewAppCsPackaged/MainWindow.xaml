<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TableViewAppCsPackaged.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TableViewAppCsPackaged"
    xmlns:tabular="using:Microsoft.UI.Xaml.Controls.Tabular"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TableViewAppCsPackaged">

    <!-- ToggleTheme_Click sets RequestedTheme on RootGrid. A local RequestedTheme override
         re-resolves ThemeResource values only on the subtree below it, not on the element
         it is set on, so the themed Background must live on the child ThemeRoot. -->
    <Grid x:Name="RootGrid">
        <Grid x:Name="ThemeRoot"
              Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
              Padding="24"
              RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="16">
                <Button Content="Toggle theme" Click="ToggleTheme_Click" />
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Instantiated from markup. -->
            <Grid Grid.Row="1" RowSpacing="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Table 1 - instantiated from MARKUP (XAML)" FontWeight="SemiBold" />
                <tabular:TableView x:Name="MarkupTable" Grid.Row="1" ItemsSource="{x:Bind People}">
                    <tabular:TableViewTextColumn Header="Name" Binding="{Binding Name}" />
                    <tabular:TableViewTextColumn Header="Age" Binding="{Binding Age}" />
                </tabular:TableView>
            </Grid>

            <!-- A second TableView is added here from code-behind. -->
            <Grid Grid.Row="2" RowSpacing="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Table 2 - created in CODE-BEHIND" FontWeight="SemiBold" />
                <StackPanel x:Name="CodeTableHost" Grid.Row="1" />
            </Grid>
        </Grid>
    </Grid>
</Window>
