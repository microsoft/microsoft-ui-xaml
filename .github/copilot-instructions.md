# Copilot Instructions for WinUI

For repository initialization, build selection, recovery, and diagnostics, follow:

- `.github/skills/build/SKILL.md`

When the user asks to build the repository without naming a target, perform the complete default
build. Do not report success based on a smaller component build.
