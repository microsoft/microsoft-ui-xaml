<Page
    x:Class="ItemsRepeaterExperiments.Container.SelectionModelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ItemsRepeaterExperiments.Container"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:common="using:ItemsRepeaterExperiments.Common"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="This page tests how you can use the HandleContainer in combination with SelectionModel"/>
            
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <CheckBox Content="Single select" IsChecked="{x:Bind selectionModel.SingleSelect, Mode=TwoWay}"/>

            <muxc:RadioButtons x:Name="LayoutChooser"
                    SelectionChanged="LayoutChooser_SelectionChanged" SelectedIndex="0">
                <x:String>Stacklayout vertical</x:String>
                <x:String>Stacklayout horizontal</x:String>
                <!-- UniformGridLayout is flaky -->
                <x:String>FlowLayout horizontal</x:String>
                <x:String>FlowLayout vertical</x:String>
            </muxc:RadioButtons>
        </StackPanel>
        
        
        <muxc:ItemsRepeaterScrollHost Grid.Row="2">
            <ScrollViewer x:Name="TeamPresenterScroller" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <muxc:ItemsRepeater x:Name="TeamPresenter" Grid.Row="2">

                    <muxc:ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="common:Team">
                            
                            <!-- Handles managing visual states, such as pressing, handling clicks etc -->
                            <local:HandleContainer Click="HandleContainer_Click">

                                <StackPanel x:Name="RootPanel" BorderBrush="Transparent" 
                                    Padding="8"
                                    BorderThickness="1" MinHeight="200" MinWidth="200">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="PointerOver">
                                                <VisualState.Setters>
                                                    <Setter Target="RootPanel.Background" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Target="RootPanel.Background" Value="{ThemeResource SystemControlBackgroundBaseMediumBrush}"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>

                                        <VisualStateGroup x:Name="SelectionStates">
                                            <VisualState x:Name="Deselected"/>
                                            <VisualState x:Name="Selected">
                                                <VisualState.Setters>
                                                    <Setter Target="RootPanel.BorderBrush" Value="{ThemeResource SystemControlBackgroundAccentBrush}"/>
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    
                                    <TextBlock Text="{x:Bind Name}" FontSize="24"/>
                                    <TextBlock Text="{x:Bind MembersString}"/>
                                </StackPanel>

                            </local:HandleContainer>
                        </DataTemplate>
                    </muxc:ItemsRepeater.ItemTemplate>
                </muxc:ItemsRepeater>
                
            </ScrollViewer>
        </muxc:ItemsRepeaterScrollHost>
    </Grid>
</Page>
