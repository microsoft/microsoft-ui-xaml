<Page
    x:Class="TestAppCX.WebView2TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestAppCX"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:mupc="using:Microsoft.UI.Private.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="url" PlaceholderText="Url" Width="400" Text="https://bing.com"/>
        <Button x:Name="go" Click="OnGo" Content="Go" Grid.Column="1"/>
        <Border Grid.ColumnSpan="3" Grid.Row="1"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderBrush="Purple" BorderThickness="2">
            <controls:WebView2 x:Name="wv2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="800" Height="600" Source="https://bing.com"/>
        </Border>
    </Grid>
</Page>
